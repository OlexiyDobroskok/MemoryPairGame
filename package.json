{
  "dependencies": {
    "autoprefixer": "^10.4.13",
    "postcss-cli": "^10.1.0",
    "postcss-csso": "^6.0.1",
    "postcss-import": "^15.1.0"
  },
  "scripts": {
    "minify-css": "postcss styles/index.css --use postcss-import --use autoprefixer --use postcss-csso --no-map --output styles/index.min.css"
  }
}
